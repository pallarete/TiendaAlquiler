@model TiendaAlquiler.Models.Foto

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Foto</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="FotoId" />
            <div class="form-group">
                <label asp-for="CocheId" class="control-label"></label>
                <select asp-for="CocheId" class="form-control" asp-items="ViewBag.CocheId"></select>
                <span asp-validation-for="CocheId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="RutaAcceso" class="control-label"></label>
                <input asp-for="RutaAcceso" class="form-control" />
                <span asp-validation-for="RutaAcceso" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
